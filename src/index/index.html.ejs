<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="index.css">
  <style>
    /* Base. */
    @font-face{font-family:"Open Sans";src:url(opensans-regular.woff2) format("woff2");
               font-weight:normal;font-style:normal}
    @font-face{font-family:"Open Sans";src:url(opensans-bold.woff2) format("woff2");
               font-weight:bold;font-style:normal}
    @font-face{font-family:"Open Sans";src:url(opensans-italic.woff2) format("woff2");
               font-weight:normal;font-style:italic}
    html,body,#tistore-index{height:100%;margin:0;background-color:#eee;font-family:"Open Sans",sans-serif}
    input:focus,button:focus{outline:none}
    button:disabled{cursor:auto!important}
    input[type=number]::-webkit-inner-spin-button,
    input[type=number]::-webkit-outer-spin-button{opacity:1;cursor:pointer}
    a[href]:hover{color:red}
    /* Ugly hack to stop shaking effect. See <https://github.com/FortAwesome/Font-Awesome/issues/671>
       for details. */
    .fa-spinner.fa-pulse{transform-origin:50% 46%}
    /* Table (because the amount of inline styles would be enormous. */
    .tistore-col:nth-child(odd){background-color:#ddd}
    .tistore-row1{width:14px;overflow:hidden;padding:2px 10px;text-align:center}
    .tistore-row2{width:50%;overflow:hidden;text-overflow:ellipsis;padding-right:15px;
                  color:#333;font-style:italic}
    .tistore-row3{overflow:hidden;text-overflow:ellipsis;text-align:center}
    .tistore-row4{width:65px;overflow:hidden;text-align:right;color:#999;font-style:italic}
    .tistore-row1-ready{color:orange}
    .tistore-row1-ok{color:green}
  </style>
</head>
<body>
  <div id="tistore-index"></div>
  <script src="index.js"></script>
</body>
</html>
